// template string is the ES6 version of the string which is used to concate the string
/*
syntax before write this syntax firstly make `backticks`
${}*/ 

let apiData="movieDbi";
const data=(apiData)=>{
    return apiData;
};

console.log(`apiData name is ${data(apiData)}`);
//template string


